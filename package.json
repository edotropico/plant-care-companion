{
  "name": "plant-care-companion",
  "version": "1.0.0",
  "description": "Progressive Web App per la cura delle piante d'appartamento: ritmi di annaffiatura, diagnosi dei problemi, storico e distintivi.",
  "author": "Edoardo Giangrandi",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --minify --format=iife --define:process.env.NODE_ENV='\"production\"' --banner:js='/*! Plant Care Companion — di Edoardo Giangrandi — © 2026, licenza MIT */' --outfile=js/app.js",
    "dev": "esbuild src/index.jsx --bundle --format=iife --outfile=js/app.js --watch",
    "serve": "python3 -m http.server 8080"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
